@page "/"
@page "/index.html"
@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        // Uncomment the following to navigate to the Common samples page on app start
        //Navigation.NavigateTo("commonSamples/Cameras-TargetPositionCameraSample", replace: true); // Replace '.' in Location with '-' (see wwwroot/Samples.xml)

        Navigation.NavigateTo("introduction", replace: true);
    }
}